package constants

const (
	//UserKey UserKey
	UserKey = iota
	//DbKey DbKey
	DbKey = iota
	//Authorization Authorization
	Authorization = "Authorization"

	//ResponseData ResponseData
	ResponseData = iota
)
